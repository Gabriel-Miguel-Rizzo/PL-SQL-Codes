  
select tabb.titulo into razao3
from(
select a.titulo titulo, rownum numero
from t_binovo_gpds_dim_projeto a
) tabb

where numero  = 5;



-----------------OBS-----------------------------

 - Usado para colocar o resultado de ROWS em VARIÁVEIS
 - (Diferente do Insert into, que é usado para colocar o resultado de um SELECT em TABELAS)
