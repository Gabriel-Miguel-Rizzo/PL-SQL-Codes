
declare
i number := 1;
var_nome varchar2(300);
numero_linhas number;

begin

select count(*) into numero_linhas
from tabela_exercicio;



loop
select tabb.nome into var_nome
from(
select te.nome nome, rownum numero
from tabela_exercicio te
) tabb

where tabb.numero  =  i;

exit when i = numero_linhas;

DBMS_OUTPUT.PUT_LINE('Nome = ' || var_nome);
i := i + 1;

end loop;

DBMS_OUTPUT.PUT_LINE('Nome = ' || var_nome);


end;

------Exercício feito por Gabriel usando : select into, loop, rownum, subquerys e DBMS_OUTPUT com finalidade de criar um Cursor Manual.
